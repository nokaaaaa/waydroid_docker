# Template only. `waydroid init` creates /var/lib/waydroid/waydroid.cfg.
# Merge only the required properties into that generated file; do not replace
# image metadata/paths with this short sample.
[properties]
# Software rendering is slower but useful on a GUI-less host without usable DRM.
ro.hardware.gralloc=default
ro.hardware.egl=swiftshader

# Do not fake Wi-Fi identity here. The actual Waydroid link is Ethernet/veth.
